spring.application.name=registration-system

# Server
server.port=8080

# Local storage path for JSON and uploaded files
app.storage.directory=./local_data

# Multipart upload limits (increase to support PDFs and images)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
